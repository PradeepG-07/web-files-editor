export enum ItemsViewType {
    LIST = 'list',
    GRID = 'grid'
}
export enum DirectoryEnum{
    SEPARATOR = "/"
}
export enum ToolBarActions{
    COPY = "copy",
    CUT = "cut",
    PASTE = "paste",
    DELETE = "delete",
}